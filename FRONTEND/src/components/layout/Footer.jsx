import { Container } from "react-bootstrap";

const Footer = () => (
  <footer className="app-footer border-top py-3 mt-auto">
    <Container fluid>
      <small className="text-muted">Valfora Holdings ERP</small>
    </Container>
  </footer>
);

export default Footer;

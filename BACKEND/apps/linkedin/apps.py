"""App config for LinkedIn integration."""

from django.apps import AppConfig


class LinkedinConfig(AppConfig):
    """Django app config for LinkedIn module."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.linkedin"

